{% for group in robots.default_groups %}
  {{- group.user_agent -}}

  {% for rule in group.rules %}
    {{- rule -}}
  {% endfor %}

  {%- if group.sitemap != blank -%}
    {{ group.sitemap }}
  {%- endif -%}
{% endfor %}

# -----------------------------------------------------
# Custom Rules: Manage AI and LLM Crawlers
# -----------------------------------------------------

# Block OpenAI's crawlers
User-agent: GPTBot
Disallow: /

User-agent: ChatGPT-User
Disallow: /

# Block Google's AI crawler
User-agent: Google-Extended
Disallow: /

# Block Anthropic's Claude
User-agent: anthropic-ai
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Claude-Web
Disallow: /

# Block Common Crawl (used by many AI models)
User-agent: CCBot
Disallow: /

# Block Perplexity
User-agent: PerplexityBot
Disallow: /

# Block Facebook/Meta's crawler
User-agent: FacebookBot
Disallow: /
